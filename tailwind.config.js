/** @type {import('tailwindcss').Config} */
export default {
  //el punto es la carpeta principal, los / es para diferencia la siguiente carpeta, los * me indican que recorren las carpetas para poder buscar el archivo requerido
  content: ["./src/**/*.{html,tsx,ts}"],
  theme: {
    extend: {},
  },
  plugins: [],
}

